Project này dùng flyway để quản lý database. Cần có 1 db trống với tên là boot_jpa,
sau đó chạy app thì nó tự động tạo bảng và chèn data vào database

Các topic trong project này:
- flyway
- JPA
- RESTful API with basic authentication (from o7planning.org)