# ============ VIEW RESOLVER ============
# Since we've already config jsp resolver in WebMvcConfig, so
# there's no need to do this anymore:
# 
# spring.mvc.view.prefix=/WEB-INF/jsp/
# spring.mvc.view.suffix=.jsp

# ============= DataSource ==================
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/guesthouse
spring.datasource.username=root
spring.datasource.password=5555

# Disable caching in the Resource chain
spring.resources.chain.cache=false
server.jsp-servlet.init-parameters.development=true

# Remove 'Using default security password' when start app
security.basic.enabled=false

# Messages. This part hasn't done yet!
welcome.message=Hello world, welcome to Spring with JSP tutorial
error.message=Error! This is an error message from applications.properties


